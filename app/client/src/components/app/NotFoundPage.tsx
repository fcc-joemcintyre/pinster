import { Typography } from '@mui/material';
import { PageContent } from '../util';

// Content displayed for 404
export const NotFoundPage = () => (
  <PageContent>
    <Typography>Sorry, could not find that page for you.</Typography>
  </PageContent>
);
