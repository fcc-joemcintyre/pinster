import styled from '@emotion/styled';

/**
 * Page layout
 * @returns Component
 */
export const PageContent = styled.div`
  max-width: 768px;
  margin: 0 auto;
  padding: 90px 4px 20px 4px;
  overflow: auto;
`;
